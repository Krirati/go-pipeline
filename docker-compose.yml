services:
  jenkins:
    build: .
    container_name: jenkins
    user: root
    group_add:
      - 999
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
volumes:
  jenkins_data: